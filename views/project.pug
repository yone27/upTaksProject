extends layout
include mixins/project
block content
    .contenido-principal
        h1=pageName

        //- pre= vardump(project)

        if errors
            each error in errors
                .alerta= error.text
        +Project(project)
                
        